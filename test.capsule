local Chat = self:import("@std/chat")
local Callback = self:import("@std/callback")

self.init = function(self)
  local myFunction = Callback:map("x, y => x * 2 + y")

  Chat:out(myFunction(3, 1)) --> 7
end