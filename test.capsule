local Type = self:import("@std/type")
local Chat = self:import("@std/chat")

self.init = function(self)
  local Dog = Type:register("Dog", {
    id = {type = "number"},
    name = {type = "string"}
  })

  local woofy = Dog:new({
    name = "Woofy"
  }) -- Error, id nem nullable, ezért szükséges deklarálni

  woofy.name = 5 -- Error, nem passzol a type
  woofy.name2 = "Woofy" -- Error, nem létezik

  -- Chat:out(woofy.name)
end