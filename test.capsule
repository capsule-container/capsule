local Chat = self:import("@std/chat")
local Array = self:import("@std/array")

self.init = function(self)
  local arr = Array:new(1, 2, 3)
  local newArr = arr:map("x => x * 5")
  Chat:out(newArr[1])
end