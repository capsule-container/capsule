local Array = self:import("@std/array")
local Chat = self:import("@std/chat")

self.init = function(self)
  local myArray = Array:new(1, 2, 3)

  local newArray = myArray:reverse()
  Chat:out(newArray:toString())
end