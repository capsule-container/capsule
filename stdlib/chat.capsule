self.out = function(self, message)
  if type(message) == "boolean" then
    message = tostring(message)
  end

  outputChatBox(message, 255, 255, 255, true)
  return true
end

self.err = function(self, message)
  outputChatBox(message, 255, 255, 255, true)
  return true
end