this.out = function(this, message)
  if type(message) == "boolean" then
    message = tostring(message)
  end

  outputChatBox(message, 255, 255, 255, true)
  return true
end

this.err = function(this, message)
  outputChatBox(message, 255, 255, 255, true)
  return true
end